# PLACEHOLDER — not a real GPG keyring.
#
# The real shedos.gpg is a binary GPG pubkey export produced by
# packaging/shedos-keyring/scripts/key-ceremony.sh. Committing it to the repo
# is fine (public keys are public). The *private* key lives only in the
# SHEDOS_REPO_SIGNING_KEY GitHub secret.
#
# Before the first release, run the ceremony script and commit the real
# shedos.gpg + shedos-trusted over these placeholders.
